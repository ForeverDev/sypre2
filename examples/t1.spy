func giv : real = (n : real) {
	return n;
}

func swag : real = (n : real, m : real) {
	return n;
}
	
// the entry point
func main : null = () {

	// should evaluate to 12
	println(30 * swag(20, 10));

	return;
}

