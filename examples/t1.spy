// the entry point
func main : null = () {

	pi : real = 3.14159265358979;

	x : real = max(5, 10, 15);
	y : real = min(5, 10, 15);
	z : real = sin(pi);
	a : real = cos(pi);
	b : real = max(pi, 2);

	println(x, y, z, a, b);
	
	return;
}

