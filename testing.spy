struct Vector3 {
	real x;
	real y;
	real z;
}

func factorial : real = (n : real) {
	if n == 1 {
		return 1;
	}
	return n * factorial(n - 1);
}

func main : null = (filepath : string) {

	x : real = factorial(5);
	y : real = factorial(6);
	z : real;
	z = factorial(7);
	
	v : new Vector3;
	v.x = 60 - 3;

	println(x, y, z, v.x);

	return 0;
}

