struct Vector3 {
	real x;
	real y;
	real z;
}

func factorial : real = (n : real) {
	if n == 1 {
		return 1;
	}
	return n * factorial(n - 1);
}

func main : null = (filepath : string) {

	x : real = factorial(5);

	println(x);

	return;
}

